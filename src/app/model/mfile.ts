export class Mfile {

    id: number;
    folder_id: number;
    filename: string;
    mtype: number;
    folder: any;

   constructor(id: number, folder_id: number, filename: string, mtype: string) {
   }

   
}